# Cheker branding assets

Generated on 2026-09-15 using the built-in `image_gen.imagegen` tool in two requests: one request for each final asset. No variants, retries, editing, external APIs, or CLI fallback were used.

## Deliverables

| Asset | File | Dimensions | Format |
| --- | --- | --- | --- |
| Compact segmented hexagonal check logomark | `cheker-logomark.png` | 1254 × 1254 | RGBA PNG, genuine transparent background |
| Document scanning brand illustration | `cheker-scan-illustration.png` | 1672 × 941 | RGB PNG, approximately 16:9 |

Both files are byte-identical copies of the selected original tool outputs. The originals remain in the generated-images directory. Exact original paths, dimensions, alpha measurements and SHA-256 digests are in `asset-metadata.json`. Exact tool prompts are in `prompts.json` under `logo` and `hero`.

## Visual inspection

The logomark has a teal check within a segmented teal and pale-silver hexagonal frame, no lettering, and real transparency. It is a raster asset with subtle generated tonal variation and semitransparent edge pixels; it is not an editable vector. The illustration shows a teal scanning plane intersecting translucent document sheets against a dark navy background, with the subject weighted to the right and clear negative space on the left. It contains no text or UI.

These are graphic branding metaphors, not evidence of software performance or a claim of universal threat detection.

This directory contains branding deliverables only. No Site checkout or application source was changed by the asset task.
